[project]
name = "headless-factory"
version = "0.1.0"
description = "Project bootstrap toolkit for Claude Code + Cursor workflows"
requires-python = ">=3.11"
dependencies = [
    "google-genai>=1.68.0",
    "python-dotenv>=1.2.2",
]
